{
  "name": "@zeidon/rest-client",
  "version": "2.1.0",
  "description": "Base classes for making Zeidon calls to REST server.  Requires an implementation.",
  "main": "dist/rest-client.js",
  "scripts": {
    "clean": "rm -f dist/*",
    "clean-all": "rm -rf dist/* node_modules",
    "build": "tsc",
    "prepare": "tsc",
    "test": "echo 'Nothing to do'"
  },
  "license": "LGPL2.1",
  "dependencies": {
    "@zeidon/core": "2.1.0"
  }
}
